# 巨量引擎按筛选结果大批量操作 PRD

## 更新记录

| 日期 | 版本 | 更新内容 | 更新人 |
| --- | --- | --- | --- |
| 2026-06-18 | V1.0 | 新增巨量引擎项目/单元层级按筛选结果大批量操作 PRD | 国民 |
| 2026-06-18 | V1.1 | 补充项目改价浅层出价限制提示与本地过滤规则 | 国民 |
| 2026-06-18 | V1.2 | 异步任务详情增加失败项批量重试规则 | 国民 |

## 一、需求背景

买量小飞机当前批量操作依赖用户先勾选对象，且单页最多展示 100 条。当项目/单元数量达到 2 万级以上时，勾选操作成本高、效率低。

本需求新增“按筛选结果批量操作”，用户通过页面筛选条件圈定操作范围，直接提交异步任务。提交前不逐条拉取详情、不逐条校验当前配置，降低巨量接口访问压力。

## 二、功能设计

### 1. 入口

入口位置：巨量引擎 `项目` / `单元` 列表页，放在原 `批量操作` 旁边。

入口名称：`按筛选结果批量操作`

| 层级 | 支持动作 |
| --- | --- |
| 项目 | 开启/关闭项目、修改预算、修改出价、修改投放时间、删除项目 |
| 单元 | 开启/关闭单元、修改预算、修改出价、删除单元 |

数据规则：
- 作用范围为当前页面筛选结果。
- 不依赖勾选项。
- 已勾选对象时，新入口仍按筛选结果生效。
- 原 `批量操作` 保持按勾选项生效，不改造。

校验规则：
- 当前筛选结果为 0 时，不允许打开或不允许提交。
- 所有操作提交前必须展示预计影响数量和筛选条件快照。

### 2. 修改预算

适用层级：项目、单元

传参字段：

| 字段 | 说明 |
| --- | --- |
| media | 巨量引擎 |
| level | 项目 / 单元 |
| filterSnapshot | 当前筛选条件快照 |
| affectedCount | 命中数量快照 |
| budgetMode | 日预算 / 不限预算 |
| budget | 日预算金额，仅日预算必填 |
| operator | 操作人 |

校验规则：
- 选择 `日预算` 时，预算金额必填。
- 预算金额必须大于 0，单位为元。
- 选择 `不限预算` 时，不展示金额输入。
- 提交前不查询当前预算。

异步规则：
- 后端按筛选条件圈定对象。
- 按 `advertiser_id` 分组。
- 每批最多 10 个对象调用巨量接口。
- 接口失败或不符合预算规则的对象记录为失败。

### 3. 修改出价

适用层级：项目、单元

项目层级接口规则：

| 字段 | 说明 |
| --- | --- |
| 官方接口 | `POST /open_api/v3.0/project/cpa_bid/update/` |
| 出价字段 | `cpa_bid` |
| 支持范围 | 仅修改项目浅层出价 |
| 不支持范围 | 不修改深层出价、ROI 系数或出价方式 |
| 单批限制 | 同一账号下每批 1～10 个项目 |

传参字段：

| 字段 | 说明 |
| --- | --- |
| media | 巨量引擎 |
| level | 项目 / 单元 |
| filterSnapshot | 当前筛选条件快照 |
| affectedCount | 命中数量快照 |
| bid | 出价金额 |
| operator | 操作人 |

校验规则：
- 出价金额必填。
- 出价金额必须大于 0，最多保留两位小数。
- 项目层级需提示：本入口仅修改浅层出价 `cpa_bid`。
- 提交前不查询当前出价。
- 提交前不逐条校验是否支持改价。

异步规则：
- 后端按筛选条件圈定对象。
- 项目层级先基于小飞机本地/缓存数据过滤不支持 `cpa_bid` 更新的项目。
- 本地过滤失败的项目不调用巨量接口，直接记录为失败。
- 可改价对象按账号分组，每批最多 10 个对象。
- 不支持浅层出价、缺少本地判断字段、接口限频、接口失败等记录为失败。

### 4. 开启/关闭

适用层级：项目、单元

传参字段：

| 字段 | 说明 |
| --- | --- |
| media | 巨量引擎 |
| level | 项目 / 单元 |
| filterSnapshot | 当前筛选条件快照 |
| affectedCount | 命中数量快照 |
| optStatus | ENABLE / DISABLE |
| operator | 操作人 |

校验规则：
- 必须选择目标操作：开启或关闭。
- 提交前不查询当前状态。
- 不按勾选对象生效。

异步规则：
- 后端按筛选结果圈定对象。
- 按账号分组，每批最多 10 个对象。
- 状态不支持、无权限、对象不存在等记录为失败。

### 5. 修改投放时间

适用层级：仅项目层级

传参字段：

| 字段 | 说明 |
| --- | --- |
| media | 巨量引擎 |
| level | 项目 |
| filterSnapshot | 当前筛选条件快照 |
| affectedCount | 命中数量快照 |
| effectiveType | 立即生效 / 次日 0 点生效 |
| scheduleType | 从今天起长期投放 / 设置结束日期 |
| endTime | 结束日期时间 |
| weekScheduleType | 不限 / 指定投放时间段 |
| weekSchedule | 7x24 小时段 |
| operator | 操作人 |

校验规则：
- 选择 `设置结束日期` 时，结束日期时间必填，精确到秒。
- 选择 `指定投放时间段` 时，必须至少选择一个小时段。
- 选择 `不限` 时，不需要传小时段明细。
- 提交前不查询当前投放日期或投放时段。

异步规则：
- 投放日期和投放时段统一作为一次“修改投放时间”任务提交。
- 后端可拆分调用对应巨量接口。
- 任一对象失败不阻塞整体任务。

### 6. 删除

适用层级：项目、单元

传参字段：

| 字段 | 说明 |
| --- | --- |
| media | 巨量引擎 |
| level | 项目 / 单元 |
| filterSnapshot | 当前筛选条件快照 |
| affectedCount | 命中数量快照 |
| operator | 操作人 |

校验规则：
- 删除必须二次确认。
- 第一步点击 `继续确认删除`。
- 第二步展示红色风险确认区。
- 用户必须勾选：`我确认已核对筛选条件，并按当前筛选结果删除全部对象`。
- 未勾选时，`提交删除任务` 禁用。
- 提交前不展示对象明细。

异步规则：
- 后端按筛选条件圈定对象。
- 按账号分组，每批最多 10 个对象。
- 删除成功计入成功数。
- 删除失败记录对象 ID 和失败原因。

### 7. 筛选条件快照

展示字段：

| 字段 | 项目层级 | 单元层级 |
| --- | --- | --- |
| 媒体 | 展示 | 展示 |
| 层级 | 展示 | 展示 |
| 广告账号 | 展示 | 展示 |
| 状态 | 项目状态 | 单元状态 |
| 营销目的 | 展示 | 展示 |
| 创建时间 | 展示 | 展示 |
| 关键词 | 展示 | 展示 |
| 数据筛选条件 | 展示 | 展示 |
| 投放模式 | 展示 | 不展示 |
| 投放日期 | 展示 | 不展示 |
| 投放时段 | 展示 | 不展示 |
| 项目出价 | 展示 | 不展示 |
| 学习期状态 | 不展示 | 展示 |

数据规则：
- 提交任务时保存筛选条件快照。
- 后续用户修改页面筛选，不影响已创建任务。
- 不提交 2 万个对象 ID 给前端。

### 8. 提交规则

通用校验：
- 命中数量为 0 禁止提交。
- 参数未填完整禁止提交。
- 提交前不逐条请求巨量详情接口。
- 提交前不批量查询当前预算、当前出价、当前状态。

成功反馈：
- 弹窗提示：异步任务已创建。
- 不展示任务 ID。
- 用户统一到任务管理页查看执行结果。

### 9. 异步任务

任务列表展示字段：

| 字段 | 说明 |
| --- | --- |
| ID | 任务 ID |
| 任务名称 | 异步任务名称 |
| 操作时间 | 任务创建时间 |
| 操作类型 | 按筛选结果修改出价等 |
| 状态 | 创建中、进行中、成功、部分成功、失败 |
| 完成时间 | 任务完成时间 |
| 操作 | 查看详情、删除 |

详情基本信息展示字段：

| 字段 | 说明 |
| --- | --- |
| 操作类型 | 当前任务操作类型 |
| 操作人 | 发起任务的用户 |
| 媒体 | 巨量引擎 |
| 层级 | 项目 / 单元 |
| 任务状态 | 当前执行状态 |
| 操作时间 | 任务创建时间 |
| 完成时间 | 任务完成时间 |
| 操作参数 | 出价、预算、开启/关闭、删除等参数 |
| 总数 | 本次任务对象总数 |
| 成功数 | 执行成功对象数 |
| 失败数 | 执行失败对象数 |

详情明细展示字段：

| 字段 | 说明 |
| --- | --- |
| 执行结果 | 成功 / 失败 |
| 广告ID/项目ID | 项目层级展示项目 ID，单元层级展示单元 ID |
| 广告名称/项目名称 | 项目层级展示项目名称，单元层级展示单元名称 |
| 账号ID | 巨量广告账号 ID |
| 账号名称 | 巨量广告账号名称 |
| 失败信息 | 失败原因 |
| 执行时间 | 单条对象执行时间 |

筛选条件：
- 支持按状态筛选。
- 支持按操作类型筛选。
- 支持任务名称 / ID 搜索。

执行过程：
- 后端基于筛选快照圈定对象。
- 按 `advertiser_id` 分组。
- 每批最多 10 个对象调用巨量接口。
- 2 万级任务通过异步队列分批执行。
- 单个对象失败不阻塞整体任务。

分包结果：
- 成功数
- 失败数
- 总数
- 无跳过状态

校验规则：
- 点击失败数，自动筛选失败明细。
- 勾选 `只查看失败任务` 后，只展示失败行。
- 失败数大于 0 时，详情页提供 `批量重试失败项`。
- 点击 `批量重试失败项` 后，仅对本任务失败明细创建重试任务，不影响成功明细。
- 明细分页加载，不一次性渲染 2 万条。
